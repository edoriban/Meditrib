
services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=meditrib
      - POSTGRES_PASSWORD=meditrib_secure_pass
      - POSTGRES_DB=meditrib_db
    restart: always

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    environment:
      - DATABASE_URL=postgresql://meditrib:meditrib_secure_pass@db/meditrib_db
      - SECRET_KEY=YOUR_SUPER_SECRET_KEY_CHANGE_ME_IN_PROD
      - BACKEND_CORS_ORIGINS=["http://localhost", "http://127.0.0.1"]
    depends_on:
      - db
    restart: always

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always

volumes:
  postgres_data:
